#include <stdio.h>

/**
 * mul - entry point for the program to check for digit 0 - 9
 * @a: first integer
 * @b: second integer
 *
 * Return: the multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
