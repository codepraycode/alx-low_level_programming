#include <stdio.h>

/**
 * main - entry point for program
 *
 * Return: 0 as a terminator
 */
int main(void)
{
	int b10;

	for (b10 = 0; b10 < 10; b10++)
	{
		printf("%d", b10);
	}

	printf("\n");
	return (0);
}
