#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 * @n: numeric value to check
 *
 * Return: int return value
 */
int _abs(int n)
{

	if (n < 0)
		return (n * -1);

	return (n);
}
